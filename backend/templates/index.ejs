<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>EdgePilot — IoT Device Manager for Teams</title>
  <meta name="description" content="EdgePilot is a lightweight, secure IoT device manager for teams — deploy, monitor, and manage edge devices at scale with real-time telemetry, secure OTA updates, and zero-touch onboarding.">
  <meta name="keywords" content="IoT device manager, device monitoring, OTA updates, edge devices, telemetry, fleet management, IoT dashboard">
  <meta name="author" content="EdgePilot Labs">

  <!-- Open Graph -->
  <meta property="og:title" content="EdgePilot — IoT Device Manager for Teams">
  <meta property="og:description" content="Deploy, monitor, and manage your edge device fleet with real-time telemetry, secure OTA, and effortless onboarding.">
  <meta property="og:type" content="website">

  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="EdgePilot — IoT Device Manager for Teams">
  <meta name="twitter:description" content="A fast, secure IoT device manager built for engineers and product teams — real-time alerts, health dashboards, and one-click OTA updates.">

  <!-- Structured data (JSON-LD) -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebApplication",
    "name": "EdgePilot",
    "url": "https://example.com/",
    "description": "EdgePilot — lightweight IoT device manager for teams. Monitor telemetry, manage firmware, and automate alerts.",
    "applicationCategory": "DeveloperApplication",
    "operatingSystem": "All",
    "aggregateRating": {
      "@type": "AggregateRating",
      "ratingValue": "4.8",
      "reviewCount": "124"
    }
  }
  </script>

  <style>
    :root{
      --bg:#0f1724; /* deep slate */
      --muted:#94a3b8;
      --accent:#2a9d8f; /* teal (from your palette) */
      --accent-2:#e76f51; /* warm accent */
      --card:#0b1220;
      --glass: rgba(255,255,255,0.03);
      --radius:12px;
      --max:1100px;
    }
    *{box-sizing:border-box}
    body{font-family:Inter,system-ui,Segoe UI,Roboto,Arial,sans-serif;margin:0;color:#e6eef8;background:linear-gradient(180deg,var(--bg),#08101a);-webkit-font-smoothing:antialiased}
    .wrap{max-width:var(--max);margin:48px auto;padding:32px}

    header{display:flex;justify-content:space-between;align-items:center;margin-bottom:28px}
    .brand{display:flex;align-items:center;gap:12px}
    .logo{width:48px;height:48px;border-radius:10px;background:linear-gradient(135deg,var(--accent),var(--accent-2));display:flex;align-items:center;justify-content:center;color:white;font-weight:700}
    .title{font-size:18px;font-weight:600}

    nav .login{padding:10px 16px;border-radius:10px;background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--accent);text-decoration:none;font-weight:600}

    .hero{display:grid;grid-template-columns:1fr 420px;gap:28px;align-items:center;margin-top:8px}
    .hero-card{background:linear-gradient(180deg,var(--card),rgba(5,10,18,0.6));padding:28px;border-radius:var(--radius);box-shadow:0 6px 24px rgba(2,6,11,0.6);backdrop-filter:blur(6px)}
    h1{margin:0;font-size:34px;line-height:1.02}
    p.lead{color:var(--muted);margin-top:10px;font-size:16px}

    .features{display:grid;grid-template-columns:repeat(2,1fr);gap:12px;margin-top:22px}
    .feature{background:var(--glass);padding:12px;border-radius:10px;border:1px solid rgba(255,255,255,0.02)}
    .feature h4{margin:0;font-size:14px}
    .feature p{margin:6px 0 0;color:var(--muted);font-size:13px}

    /* right column visual */
    .panel{background:linear-gradient(180deg,rgba(255,255,255,0.02),transparent);padding:18px;border-radius:10px;border:1px solid rgba(255,255,255,0.03)}
    .screenshot{height:220px;border-radius:8px;background:linear-gradient(180deg,#051728,#08202a);display:flex;flex-direction:column;gap:8px;padding:16px;color:var(--muted)}
    .screenshot h3{margin:0;color:#dff3ef}
    .screenshot .metrics{display:flex;gap:8px;margin-top:10px}
    .metric{flex:1;background:rgba(255,255,255,0.02);padding:10px;border-radius:8px;text-align:center}
    .metric strong{display:block;font-size:20px;color:white}
    .cta{display:flex;gap:12px;margin-top:18px}
    .btn-primary{background:linear-gradient(90deg,var(--accent),#188f83);padding:12px 16px;border-radius:10px;color:white;text-decoration:none;font-weight:700;border:none}

    .stats{display:flex;gap:12px;margin-top:28px}
    .stat{background:rgba(255,255,255,0.02);padding:12px;border-radius:10px;text-align:center;flex:1}
    .stat h4{margin:0;color:white}
    .stat p{margin:6px 0 0;color:var(--muted)}

    .section{margin-top:44px}
    .cards{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}
    .card{background:linear-gradient(180deg,rgba(255,255,255,0.02),transparent);padding:18px;border-radius:12px;border:1px solid rgba(255,255,255,0.03)}

    footer{margin-top:48px;color:var(--muted);font-size:13px;text-align:center}

    /* responsive */
    @media (max-width:980px){.hero{grid-template-columns:1fr;}.cards{grid-template-columns:1fr 1fr}.wrap{margin:28px auto;padding:20px}}
    @media (max-width:640px){.cards{grid-template-columns:1fr}.hero{gap:14px}.title{font-size:16px}h1{font-size:26px}}
  </style>
</head>
<body>
  <div class="wrap" role="main">
    <header>
      <div class="brand">
        <div class="logo" aria-hidden>EP</div>
        <div>
          <div class="title">EdgePilot</div>
          <div style="font-size:12px;color:var(--muted)">IoT device manager — secure, fast, and developer-friendly</div>
        </div>
      </div>

      <!-- ONLY LINK: login -->
      <nav aria-label="Primary">
        <a class="login" href="/login">Log in</a>
      </nav>
    </header>

    <section class="hero">
      <div class="hero-card">
        <h1>Manage your edge fleet with confidence</h1>
        <p class="lead">EdgePilot puts device health, secure OTA updates, and real-time telemetry at the center of your workflow — built for engineering teams that need reliability and scale without the complexity.</p>

        <div class="features" aria-hidden>
          <div class="feature">
            <h4>Zero-touch onboarding</h4>
            <p>Pre-provision devices during manufacturing or use QR-based onboarding for field technicians.</p>
          </div>
          <div class="feature">
            <h4>Secure OTA updates</h4>
            <p>Sign firmware with keys, schedule phased rollouts and rollback safely on failure.</p>
          </div>
          <div class="feature">
            <h4>Real-time telemetry</h4>
            <p>Live metrics, alerting, and historical trends to understand device behavior quickly.</p>
          </div>
          <div class="feature">
            <h4>Role-based access</h4>
            <p>Granular permissions for operators, developers, and auditors with audit logs.</p>
          </div>
        </div>

        <div class="cta">
          <a class="btn-primary" href="/login">Start free trial — Log in</a>
          <!-- intentionally not linking anywhere else -->
          <div style="align-self:center;color:var(--muted);font-size:13px">No credit card required</div>
        </div>

        <div class="stats" role="list" aria-label="Key stats">
          <div class="stat" role="listitem">
            <h4>10k+</h4>
            <p>Devices managed</p>
          </div>
          <div class="stat" role="listitem">
            <h4>99.98%</h4>
            <p>Average uptime</p>
          </div>
          <div class="stat" role="listitem">
            <h4>4.8 ★</h4>
            <p>Avg. customer rating</p>
          </div>
        </div>
      </div>

      <aside class="panel" aria-label="Product snapshot">
        <div class="screenshot" role="img" aria-label="Dashboard snapshot">
          <h3>Live Fleet Snapshot</h3>
          <div style="font-size:13px;color:var(--muted)">Overview · Alerts · Recent OTA</div>

          <div class="metrics">
            <div class="metric">
              <small>Online</small>
              <strong>9,480</strong>
            </div>
            <div class="metric">
              <small>Offline</small>
              <strong>512</strong>
            </div>
            <div class="metric">
              <small>Pending OTA</small>
              <strong>28</strong>
            </div>
          </div>

          <div style="margin-top:auto;color:var(--muted);font-size:12px">Secure connection • TLS 1.3 • Signed firmware</div>
        </div>

        <div style="margin-top:12px;display:flex;gap:8px">
          <div style="flex:1;background:rgba(255,255,255,0.02);padding:10px;border-radius:8px;text-align:center">
            <div style="font-size:11px;color:var(--muted)">Avg. alert time</div>
            <div style="font-weight:700;color:white">18s</div>
          </div>
          <div style="flex:1;background:rgba(255,255,255,0.02);padding:10px;border-radius:8px;text-align:center">
            <div style="font-size:11px;color:var(--muted)">Mean time to fix</div>
            <div style="font-weight:700;color:white">4h 12m</div>
          </div>
        </div>
      </aside>
    </section>

    <section class="section">
      <h2 style="color:white;margin:0 0 12px 0">Why teams choose EdgePilot</h2>
      <div class="cards">
        <div class="card">
          <h3 style="margin:0 0 8px 0">Built for scale</h3>
          <p style="margin:0;color:var(--muted)">Designed to orchestrate thousands of devices with low-latency telemetry ingestion and efficient OTA pipelines.</p>
        </div>
        <div class="card">
          <h3 style="margin:0 0 8px 0">Privacy & security</h3>
          <p style="margin:0;color:var(--muted)">End-to-end encryption, hardware-backed key storage, and immutable audit logs keep your fleet secure.</p>
        </div>
        <div class="card">
          <h3 style="margin:0 0 8px 0">Developer-friendly</h3>
          <p style="margin:0;color:var(--muted)">Open APIs, SDKs for common platforms, and CLI tools let your team automate deployments and diagnostics.</p>
        </div>
      </div>
    </section>

    <section class="section">
      <h2 style="color:white;margin:0 0 12px 0">Customer stories</h2>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
        <div class="card">
          <strong>"EdgePilot cut our debugging time in half"</strong>
          <p style="margin:8px 0 0;color:var(--muted)">— Hardware lead at a smart meter company. Live telemetry, customizable alerts and rollback saved days during a firmware campaign.</p>
        </div>
        <div class="card">
          <strong>"Zero-touch onboarding made field installs painless"</strong>
          <p style="margin:8px 0 0;color:var(--muted)">— Operations manager at a logistics startup.</p>
        </div>
      </div>
    </section>

    <section class="section">
      <h2 style="color:white;margin:0 0 12px 0">Frequently asked questions</h2>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
        <div class="card">
          <strong>How is firmware signed?</strong>
          <p style="margin:8px 0 0;color:var(--muted)">Firmwares are signed using your private keys; EdgePilot validates signatures on-device before applying updates.</p>
        </div>
        <div class="card">
          <strong>Can I host EdgePilot on-prem?</strong>
          <p style="margin:8px 0 0;color:var(--muted)">Yes — EdgePilot supports cloud and self-hosted deployments with the same APIs and tooling.</p>
        </div>
      </div>
    </section>

    <footer>
      <p>© <span id="year"></span> EdgePilot Labs. <span style="color:var(--muted)">A demo IoT device manager for assignment use.</span></p>
    </footer>
  </div>

  <script>
    // accessibility: set current year
    document.getElementById('year').textContent = new Date().getFullYear();

    // lightweight animation for entrance (no external libs)
    document.querySelectorAll('.hero-card, .panel, .card').forEach((el,i)=>{
      el.style.opacity = 0; el.style.transform='translateY(8px)';
      setTimeout(()=>{el.style.transition='opacity .45s ease,transform .45s ease';el.style.opacity=1;el.style.transform='none'}, 120*i);
    });
  </script>
</body>
</html>
