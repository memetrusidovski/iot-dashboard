{
  "name": "fullstack",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently -n sim,backend,frontend -c bgGreen.white,bgBlue.white,bgMagenta.white \"npm run mqtt\" \"npm run server\" \"npm run client\"",
    "server": "node backend/server.js",
    "client": "npm run dev --prefix frontend",
    "mqtt": "node backend/mqtt-simulator.js",
    "install-client": "npm install --prefix frontend",
    "install-server": "npm install --prefix backend",
    "postinstall": "npm install && npm run install-client && npm run install-server"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1"
  }
}
